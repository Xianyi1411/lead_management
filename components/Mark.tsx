// The pipeline logo mark: five ascending bars in the stage colours, New → Won —
// the funnel's happy path (DESIGN.md — the identity is the pipeline).
export default function Mark() {
  return (
    <span className="mark" aria-hidden="true">
      <i /><i /><i /><i /><i />
    </span>
  );
}
